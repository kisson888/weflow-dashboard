# WeFlow 监控看板开发完成

## 新增文件清单

| 文件 | 说明 |
|------|------|
| `dashboard.html` | 前端看板页面 — 6 个标签页 | 
| `dashboard_server.py` | Python API 后端 — 端口 8765 |
| `start_dashboard.bat` | 一键启动脚本（双击运行） |

## 看板功能总览

### 总览标签页
- WeFlow 在线状态指示器
- 4 个统计卡片：监控会话数、累积消息数、数据文件数、日报数
- 会话同步状态列表（今日同步/昨日同步/××小时前）
- 最新日报快捷入口

### 会话管理标签页
- 列表显示所有会话：名称、开关、最后消息时间、消息数、同步状态、ID
- **一键开关**：点击切换启用/禁用，自动保存到 config.json

### 手动执行标签页
- **增量导出**按钮 — 拉取 checkpoint 之后的新消息
- **回捞历史** — 可指定天数，适合首次部署补数据
- 执行日志区 — 实时显示脚本输出

### 日报浏览标签页
- 按日期排列的所有日报
- 点击可在线查看完整日报内容

### 数据浏览标签页
- 按会话查看数据文件列表
- 点击日期可查看原始消息 JSON 数据

### 设置标签页
- API 地址、Token、数据目录编辑
- 系统信息展示（脚本路径、配置文件等）

## 启动方式
```
方式一：双击 start_dashboard.bat（自动打开浏览器）
方式二：python dashboard_server.py
        然后访问 http://127.0.0.1:8765
```
