# WeFlow Dashboard v25 — 成品打包前优化

## 改动清单

### 1. 修复会话管理页"最后消息时间"
- **问题**: 前端用 `last_timestamp`（Unix时间戳）但后端发的是 `last_message_time`（格式化字符串）
- **修复**: 增加 fallback，先用 `last_message_time`，再回退到 `last_timestamp`

### 2. 总览页优化
- **排序**: 监控中的会话排在前面
- **标注**: 监控中 → 绿色 `监控中` 标签；待监控 → 灰色 `待监控` 标签（之前待监控不显示，监控中也不显示）

### 3. 会话管理页排序
- 监控中的会话排在前面（同总览页一致）

### 4. 设置页系统信息更新
- 移除硬编码路径（`e:\WorkBuddy\weflow-monitor\`）
- 显示通用文件名称（导出引擎、前端页面、数据文件等）
- 版本号从 `/api/health` 动态获取并实时显示

### 5. 使用说明更新
- 新增"总览"页面功能介绍
- 新增"定时导出"说明章节
- 更新页面功能描述
- 补充启动脚本说明
